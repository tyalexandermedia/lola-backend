# Domain migration — coachtyalexander.com → coachtyleads.com

**Date:** 2026-09-11
**Branch:** `claude/peaceful-einstein-n1j385`
**What:** Move the marketing site to its own domain and rename the customer-facing
**company** from "Lola Leads" to **Coach Ty Leads**.

> Brand architecture this move sets up:
> - **coachtyleads.com** → the marketing / local-growth business (this site).
> - **coachtyalexander.com** → freed up to become a separate personal-training /
>   basketball / HYROX / Everyday Athlete site later, in its **own** Vercel project.
> - Customer-facing brand: **Coach Ty Leads** · tagline **"Get found. Get called. Get booked."**
> - Product/system stays **"The Lola Local Growth System"** (and "Lola" the dog).
> - Legal entity **Ty Alexander Media LLC** only where legally appropriate (schema `legalName`).

---

## The mechanism (one variable)

The origin is defined **once** and read everywhere:

- `frontend/src/lib/pageMeta.ts` → `SITE_ORIGIN` (canonicals, `og:url`, every schema `@id`, `<title>`s)
- `frontend/scripts/prerender.mjs` → `SITE`
- `frontend/scripts/check-seo.mjs` → `SITE`
- `frontend/scripts/gen_lp.py` → `BASE_URL` (all 48 `/lp/*` pages, `sitemap.xml`, `vercel.json`)

All four read **`VITE_SITE_ORIGIN`** and fall back to a default. This migration
**flips the default** to `https://www.coachtyleads.com` and keeps the env var as the
override (that is also the rollback lever — see below).

---

## Files changed

**Origin default flipped:** `pageMeta.ts`, `prerender.mjs`, `check-seo.mjs`, `gen_lp.py`
**Hardcoded literals:** `frontend/index.html` (canonical/OG/JSON-LD), `frontend/public/robots.txt` (Sitemap line)
**Regenerated by `python3 frontend/scripts/gen_lp.py`:** 48 `frontend/public/lp/*-seo-*.html`, `industries.html`, `sitemap.xml`, `vercel.json`
**Hand-built static page fixed manually:** `frontend/public/lp/roadmap.html`
**Company rename → Coach Ty Leads:** `pageMeta.ts` (`SITE_NAME`), `index.html`, `site.webmanifest`, `lib/lola.ts`, `App.tsx` (wordmark), `Start.tsx`, `Vsl.tsx`, `VsPage.tsx`, `PricingPage.tsx`, `RetainerPage.tsx`, `AuditFlow.tsx`, `BeforeAfter.tsx`, `gen_lp.py`
**Homepage positioning:** `Homepage.tsx` hero subhead now leads with the tagline + home-service descriptor
**Comments only (docs accuracy):** `checkout.ts`, `feedback.html`

Schema keeps `alternateName: ["The Lola Local Growth System", "Lola"]` so AI-answer
citation continuity survives the name change.

---

## Redirect map

**In code (`vercel.json`, host-aware — generated by `gen_lp.py`):**

| From (old domain) | To | Type |
|---|---|---|
| `coachtyalexander.com/<any non-home path>` | `https://www.coachtyleads.com/<same path>` | 301 |
| `www.coachtyalexander.com/<any non-home path>` | `https://www.coachtyleads.com/<same path>` | 301 |

- Implemented as `{"source":"/:path+","has":[{"type":"host","value":"<old host>"}], ...}`.
  `:path+` matches **one-or-more** segments, so the bare homepage `/` is **never** caught.
- **Host-scoped:** these fire only when the request host is the old domain, so they are
  **inert** on coachtyleads.com and on `*.vercel.app` previews.
- Existing path redirects (vanity slugs, `/grader`→`/growth-score`, `/retainer`→`/pricing`,
  etc.) are **domain-agnostic** and unchanged.

**Temporary homepage redirect (during migration):** the old homepage is intentionally
NOT redirected in code. During the switch, redirect the whole of coachtyalexander.com to
coachtyleads.com at the **Vercel Domains level** (Domains → Redirect), so it can be lifted
without a code change when the training site is ready.

**After the training site launches:** coachtyalexander.com moves to its own (new) project.
Port the two host-aware legacy-path redirects above into that project (or as Vercel
domain-level redirects) so already-indexed/printed marketing URLs keep resolving to
coachtyleads.com, while the new homepage + training paths stay on coachtyalexander.com.

---

## Environment variables requiring manual updates

| Where | Variable | Action |
|---|---|---|
| **Vercel** project `lola-seo` | `VITE_SITE_ORIGIN` | If set to the old domain, change to `https://www.coachtyleads.com` **or unset** (code default is now the new domain). This is the only thing that can make a build serve the old canonical. |
| **Railway** backend | `PUBLIC_APP_URL` | Set to `https://www.coachtyleads.com` (default in `main.py` is still the old domain). |
| **Railway** backend | CORS allowlist (`main.py`) | Add `https://www.coachtyleads.com` (keep the old origin until the old domain is retired). |

---

## Authenticated / external services requiring browser changes (LAUNCH phase — not done here)

- **Vercel:** add `coachtyleads.com` + `www.coachtyleads.com` to the `lola-seo` project; set
  `www` as the canonical production domain; apex → `www` redirect; verify SSL.
- **Stripe:** update the $397/mo Payment Link success redirect from
  `coachtyalexander.com/start?session_id=...` → `coachtyleads.com/start?...`
  (documented in `frontend/src/lib/checkout.ts`). Do **not** change prices/products.
- **Google Search Console:** add + verify `coachtyleads.com`; submit
  `https://www.coachtyleads.com/sitemap.xml`; request indexing for `/`, `/growth-score`,
  `/pricing`, `/case-studies/sandbar`, primary service page; submit Change of Address only
  if its requirements match.
- **GA4:** update the property's website URL — same property, keep events + history.
- **GoHighLevel:** website URLs, allowed domains, forms, calendar/embed origins, webhook
  allowlists — only where required; preserve contacts, UTMs, pipelines, workflows.
- **Business profiles:** update the marketing business's website link to coachtyleads.com.
  Do **not** touch Sandbar Soft Wash / Tampa Bay Power Clean or other client profiles.

---

## Rollback

1. **Complete rollback — `git revert`.** Revert the migration commit(s) on this branch and
   redeploy. This is the reliable path: it restores the old code defaults, the old committed
   `/lp/*.html` + `sitemap.xml`, AND removes the host-aware `vercel.json` redirect — all
   together, so nothing is left half-migrated.
2. **Emergency, canonical-only (PARTIAL — not a full rollback).** Setting
   `VITE_SITE_ORIGIN=https://www.coachtyalexander.com` and redeploying flips the
   **prerendered app routes** (`/`, `/pricing`, `/vs/*`, …) — canonical, `og:url`, schema —
   back immediately. It does **not**:
   - regenerate the committed `/lp/*.html` or `sitemap.xml` — those are `gen_lp.py` output and
     the Vercel build never runs `gen_lp.py`, so they keep the new domain and `check-seo` will
     **fail the build** on the route/sitemap domain mismatch; and
   - change `vercel.json`, so the host-aware 301 still sends `coachtyalexander.com/<path>` to
     `coachtyleads.com` — the old domain can't serve its own content.

   So use it only as a stopgap for app-route canonicals, then finish with `git revert`. (If you
   must stay forward instead: re-run `python3 frontend/scripts/gen_lp.py` with `VITE_SITE_ORIGIN`
   set to the old origin, commit the regenerated `/lp` + `sitemap.xml`, and drop the host-aware
   redirect from `gen_lp.py`.)

## Intentionally NOT changed

Backend live flows (GHL/Stripe/tracking — launch phase), client sites (Sandbar, Tampa Bay
Power Clean), `tyalexandermedia.com` parent-org links, and anything involving invented
testimonials/metrics (there are none in the code, by design).
